{% extends 'instructorbase.html.twig' %}

{% block title %}Lessen{% endblock %}

{% block body %}
    <h1>Lessen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lessen.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ lessen.date ? lessen.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Time</th>
                <td>{{ lessen.time ? lessen.time|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ lessen.location }}</td>
            </tr>
            <tr>
                <th>Max_persons</th>
                <td>{{ lessen.maxPersons }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lessen_index') }}">back to list</a>

    <a href="{{ path('app_lessen_edit', {'id': lessen.id}) }}">edit</a>

    {{ include('instructor/lessen/_delete_form.html.twig') }}
{% endblock %}
