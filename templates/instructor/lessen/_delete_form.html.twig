<form method="post" action="{{ path('app_lessen_delete', {'id': lessen.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lessen.id) }}">
    <br>
    <button class="btn btn-danger">Delete</button>
</form>
